{
  "enable_text_update_trigger": {
    "description": "启用文本触发更新",
    "hint": "开启后，用户可发送“水鱼更新 SGWCMAID...”来更新水鱼。关闭后只保留 maimaiupdate <SGID> 等 AstrBot 命令触发，避免和其它插件或群聊文本误触发。",
    "type": "bool",
    "default": true
  },
  "text_update_command": {
    "description": "文本更新触发词",
    "hint": "默认是“水鱼更新”。实际用法为：水鱼更新 SGWCMAID...。只有启用文本触发更新时生效。",
    "type": "string",
    "default": "水鱼更新"
  },
  "enable_clear_command": {
    "description": "启用清空水鱼命令",
    "hint": "启用后可使用 maimaiclear 确认清空 向水鱼发送清空成绩请求，用于误用他人 SGID 导致成绩污染后的手动处理。",
    "type": "bool",
    "default": true
  },
  "sgid_max_age_seconds": {
    "description": "SGID 最大有效时间",
    "hint": "根据 SGID 内嵌时间戳判断是否过期。超过该秒数会拒绝使用，避免复用旧二维码导致成绩异常。",
    "type": "int",
    "slider": {
      "min": 60,
      "max": 300,
      "step": 10
    },
    "default": 180
  },
  "request_timeout_seconds": {
    "description": "网络请求超时时间",
    "hint": "请求舞萌机台服务和水鱼接口的超时时间。",
    "type": "int",
    "slider": {
      "min": 5,
      "max": 60,
      "step": 5
    },
    "default": 30
  },
  "maimai_http_proxy": {
    "description": "舞萌请求代理",
    "hint": "可选 HTTP 代理地址，例如 http://127.0.0.1:7890。留空则直连。",
    "type": "string",
    "default": ""
  },
  "kook_token": {
    "description": "KOOK Bot Token",
    "hint": "用于群聊撤回含 SGID/水鱼 Token 的消息。留空时会尝试从 KOOK 平台配置读取。",
    "type": "string",
    "default": ""
  },
  "warn_unsupported_recall": {
    "description": "撤回提示",
    "hint": "群聊收到 SGID 或水鱼 Token 后，会单独发送“已尝试撤回消息，如果没撤回请手动撤回”的安全提示。私聊不会撤回。",
    "type": "bool",
    "default": true
  }
}
